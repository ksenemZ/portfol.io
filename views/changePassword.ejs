<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <title>Смена пароля</title>
    <link href="/css/output.css" rel="stylesheet">
    <link rel="icon" href="/uploads/LOGO.png" type="image/x-icon">
</head>
<body class="bg-gray-100 flex justify-center items-center h-screen">
<div class="max-w-md w-full bg-white mt-20 p-6 rounded shadow-md">

    <h2 class="text-2xl font-bold mb-4">Смена пароля</h2>

    <% if (error) { %>
        <p class="text-red-500 text-sm font-medium"><%= error %></p>
    <% } %>
    <% if (success) { %>
        <p class="text-green-600 text-sm font-medium"><%= success %></p>
    <% } %>

    <form method="POST" action="/profile/<%= user.id %>/change-password" class="space-y-4">
        <input type="hidden" name="_csrf" value="<%= csrfToken %>">

        <div>
            <label class="block text-sm font-medium">Текущий пароль:</label>
            <input type="password" name="currentPassword" required class="w-full p-2 border rounded">
        </div>

        <div>
            <label class="block text-sm font-medium">Новый пароль:</label>
            <input type="password" name="newPassword" required class="w-full p-2 border rounded">
        </div>

        <div>
            <label class="block text-sm font-medium">Подтвердите новый пароль:</label>
            <input type="password" name="confirmPassword" required class="w-full p-2 border rounded">
        </div>

        <button type="submit" class="bg-blue-500 text-white px-4 py-2 rounded w-full">Сменить пароль</button>
    </form>

    <a href="/profile/<%= user.id %>" class="block text-center text-blue-500 mt-4">Назад</a>
</div>
</body>
</html>
