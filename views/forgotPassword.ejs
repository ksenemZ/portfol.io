<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Восстановление пароля</title>
    <link href="/css/output.css" rel="stylesheet">
    <link rel="icon" href="/uploads/LOGO.png" type="image/x-icon">
</head>
<body class="bg-gray-100 flex items-center justify-center h-screen">
<div class="bg-white p-6 rounded shadow-md w-96">
    <h2 class="text-xl font-bold mb-4">Восстановление пароля</h2>

    <% if (error) { %>
        <p class="text-red-500 mb-2"><%= error %></p>
    <% } %>
    <% if (success) { %>
        <p class="text-green-600 mb-2"><%= success %></p>
    <% } %>

    <form action="/forgot" method="POST" class="space-y-4">
        <input type="hidden" name="_csrf" value="<%= csrfToken %>">
        <input type="email" name="email" required placeholder="Введите ваш email" class="w-full p-2 border rounded">
        <button type="submit" class="w-full bg-blue-500 text-white py-2 rounded">Отправить</button>
    </form>

    <a href="/login" class="block text-center text-blue-500 mt-4">Назад к входу</a>
</div>
</body>
</html>
